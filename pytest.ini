[pytest]
testpaths = tests
# Show short tracebacks; -v for verbose cut list
addopts = --tb=short
# Markers used in the suite
markers =
    slow: marks tests that take >10s (media generation + rendering)
